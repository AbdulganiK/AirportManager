package model;
// original file: https://github.com/TakahikoKawasaki/nv-i18n/blob/master/src/main/java/com/neovisionaries/i18n/CountryCode.java
public enum Country {
    AC(Continent.AF), // Ascension Island
    AD(Continent.EU), // Andorra
    AE(Continent.AS), // United Arab Emirates
    AF(Continent.AS), // Afghanistan
    AG(Continent.NA), // Antigua and Barbuda
    AI(Continent.NA), // Anguilla
    AL(Continent.EU), // Albania
    AM(Continent.AS), // Armenia
    AO(Continent.AF), // Angola
    AQ(Continent.AN), // Antarctica
    AR(Continent.SA), // Argentina
    AS(Continent.OC), // American Samoa
    AT(Continent.EU), // Austria
    AU(Continent.OC), // Australia
    AW(Continent.NA), // Aruba
    AX(Continent.EU), // Åland Islands
    AZ(Continent.AS), // Azerbaijan
    BA(Continent.EU), // Bosnia and Herzegovina
    BB(Continent.NA), // Barbados
    BD(Continent.AS), // Bangladesh
    BE(Continent.EU), // Belgium
    BF(Continent.AF), // Burkina Faso
    BG(Continent.EU), // Bulgaria
    BH(Continent.AS), // Bahrain
    BI(Continent.AF), // Burundi
    BJ(Continent.AF), // Benin
    BM(Continent.NA), // Bermuda
    BN(Continent.AS), // Brunei Darussalam
    BO(Continent.SA), // Bolivia
    BQ(Continent.NA), // Bonaire, Sint Eustatius, and Saba
    BR(Continent.SA), // Brazil
    BS(Continent.NA), // Bahamas
    BT(Continent.AS), // Bhutan
    BV(Continent.AN), // Bouvet Island
    BW(Continent.AF), // Botswana
    BY(Continent.EU), // Belarus
    BZ(Continent.NA), // Belize

    // Additional countries to cover all ISO 3166-1 entries
    CA(Continent.NA), // Canada
    CC(Continent.AS), // Cocos (Keeling) Islands
    CD(Continent.AF), // Congo (DRC)
    CF(Continent.AF), // Central African Republic
    CG(Continent.AF), // Congo (Republic)
    CH(Continent.EU), // Switzerland
    CI(Continent.AF), // Côte d'Ivoire
    CK(Continent.OC), // Cook Islands
    CL(Continent.SA), // Chile
    CM(Continent.AF), // Cameroon
    CN(Continent.AS), // China
    CO(Continent.SA), // Colombia
    CR(Continent.NA), // Costa Rica
    CU(Continent.NA), // Cuba
    CV(Continent.AF), // Cape Verde
    CY(Continent.EU), // Cyprus
    CZ(Continent.EU), // Czech Republic
    DE(Continent.EU), // Germany
    DJ(Continent.AF), // Djibouti
    DK(Continent.EU), // Denmark
    DM(Continent.NA), // Dominica
    DO(Continent.NA), // Dominican Republic
    DZ(Continent.AF), // Algeria
    EC(Continent.SA), // Ecuador
    EE(Continent.EU), // Estonia
    EG(Continent.AF), // Egypt
    EH(Continent.AF), // Western Sahara
    ER(Continent.AF), // Eritrea
    ES(Continent.EU), // Spain
    ET(Continent.AF), // Ethiopia
    FI(Continent.EU), // Finland
    FJ(Continent.OC), // Fiji
    FK(Continent.SA), // Falkland Islands
    FM(Continent.OC), // Micronesia
    FO(Continent.EU), // Faroe Islands
    FR(Continent.EU), // France
    GA(Continent.AF), // Gabon
    GB(Continent.EU), // United Kingdom
    GD(Continent.NA), // Grenada
    GE(Continent.AS), // Georgia
    GF(Continent.SA), // French Guiana
    GG(Continent.EU), // Guernsey
    GH(Continent.AF), // Ghana
    GI(Continent.EU), // Gibraltar
    GL(Continent.NA), // Greenland
    GM(Continent.AF), // Gambia
    GN(Continent.AF), // Guinea
    GP(Continent.NA), // Guadeloupe
    GQ(Continent.AF), // Equatorial Guinea
    GR(Continent.EU), // Greece
    GT(Continent.NA), // Guatemala
    GU(Continent.OC), // Guam
    GW(Continent.AF), // Guinea-Bissau
    GY(Continent.SA), // Guyana
    HK(Continent.AS), // Hong Kong
    HM(Continent.AN), // Heard Island and McDonald Islands
    HN(Continent.NA), // Honduras
    HR(Continent.EU), // Croatia
    HT(Continent.NA), // Haiti
    HU(Continent.EU), // Hungary
    ID(Continent.AS), // Indonesia
    IE(Continent.EU), // Ireland
    IL(Continent.AS), // Israel
    IM(Continent.EU), // Isle of Man
    IN(Continent.AS), // India
    IO(Continent.AS), // British Indian Ocean Territory
    IQ(Continent.AS), // Iraq
    IR(Continent.AS), // Iran
    IS(Continent.EU), // Iceland
    IT(Continent.EU), // Italy
    JE(Continent.EU), // Jersey
    JM(Continent.NA), // Jamaica
    JO(Continent.AS), // Jordan
    JP(Continent.AS), // Japan
    KE(Continent.AF), // Kenya
    KG(Continent.AS), // Kyrgyzstan
    KH(Continent.AS), // Cambodia
    KI(Continent.OC), // Kiribati
    KM(Continent.AF), // Comoros
    KN(Continent.NA), // Saint Kitts and Nevis
    KP(Continent.AS), // North Korea
    KR(Continent.AS), // South Korea
    KW(Continent.AS), // Kuwait
    KY(Continent.NA), // Cayman Islands
    KZ(Continent.AS), // Kazakhstan
    LA(Continent.AS), // Laos
    LB(Continent.AS), // Lebanon
    LC(Continent.NA), // Saint Lucia
    LI(Continent.EU), // Liechtenstein
    LK(Continent.AS), // Sri Lanka
    LR(Continent.AF), // Liberia
    LS(Continent.AF), // Lesotho
    LT(Continent.EU), // Lithuania
    LU(Continent.EU), // Luxembourg
    LV(Continent.EU), // Latvia
    LY(Continent.AF), // Libya
    MA(Continent.AF), // Morocco
    MC(Continent.EU), // Monaco
    MD(Continent.EU), // Moldova
    ME(Continent.EU), // Montenegro
    MF(Continent.NA), // Saint Martin (French part)
    MG(Continent.AF), // Madagascar
    MH(Continent.OC), // Marshall Islands
    MK(Continent.EU), // North Macedonia
    ML(Continent.AF), // Mali
    MM(Continent.AS), // Myanmar
    MN(Continent.AS), // Mongolia
    MO(Continent.AS), // Macao
    MP(Continent.OC), // Northern Mariana Islands
    MQ(Continent.NA), // Martinique
    MR(Continent.AF), // Mauritania
    MS(Continent.NA), // Montserrat
    MT(Continent.EU), // Malta
    MU(Continent.AF), // Mauritius
    MV(Continent.AS), // Maldives
    MW(Continent.AF), // Malawi
    MX(Continent.NA), // Mexico
    MY(Continent.AS), // Malaysia
    MZ(Continent.AF), // Mozambique
    NA(Continent.AF), // Namibia
    NC(Continent.OC), // New Caledonia
    NE(Continent.AF), // Niger
    NF(Continent.OC), // Norfolk Island
    NG(Continent.AF), // Nigeria
    NI(Continent.NA), // Nicaragua
    NL(Continent.EU), // Netherlands
    NO(Continent.EU), // Norway
    NP(Continent.AS), // Nepal
    NR(Continent.OC), // Nauru
    NU(Continent.OC), // Niue
    NZ(Continent.OC), // New Zealand
    OM(Continent.AS), // Oman
    PA(Continent.NA), // Panama
    PE(Continent.SA), // Peru
    PF(Continent.OC), // French Polynesia
    PG(Continent.OC), // Papua New Guinea
    PH(Continent.AS), // Philippines
    PK(Continent.AS), // Pakistan
    PL(Continent.EU), // Poland
    PM(Continent.NA), // Saint Pierre and Miquelon
    PN(Continent.OC), // Pitcairn
    PR(Continent.NA), // Puerto Rico
    PS(Continent.AS), // Palestine
    PT(Continent.EU), // Portugal
    PW(Continent.OC), // Palau
    PY(Continent.SA), // Paraguay
    QA(Continent.AS), // Qatar
    RE(Continent.AF), // Réunion
    RO(Continent.EU), // Romania
    RS(Continent.EU), // Serbia
    RU(Continent.EU), // Russia
    RW(Continent.AF), // Rwanda
    SA(Continent.AS), // Saudi Arabia
    SB(Continent.OC), // Solomon Islands
    SC(Continent.AF), // Seychelles
    SD(Continent.AF), // Sudan
    SE(Continent.EU), // Sweden
    SG(Continent.AS), // Singapore
    SH(Continent.AF), // Saint Helena
    SI(Continent.EU), // Slovenia
    SJ(Continent.EU), // Svalbard and Jan Mayen
    SK(Continent.EU), // Slovakia
    SL(Continent.AF), // Sierra Leone
    SM(Continent.EU), // San Marino
    SN(Continent.AF), // Senegal
    SO(Continent.AF), // Somalia
    SR(Continent.SA), // Suriname
    SS(Continent.AF), // South Sudan
    ST(Continent.AF), // São Tomé and Príncipe
    SV(Continent.NA), // El Salvador
    SX(Continent.NA), // Sint Maarten
    SY(Continent.AS), // Syria
    SZ(Continent.AF), // Eswatini
    TC(Continent.NA), // Turks and Caicos Islands
    TD(Continent.AF), // Chad
    TF(Continent.AN), // French Southern Territories
    TG(Continent.AF), // Togo
    TH(Continent.AS), // Thailand
    TJ(Continent.AS), // Tajikistan
    TK(Continent.OC), // Tokelau
    TL(Continent.AS), // Timor-Leste
    TM(Continent.AS), // Turkmenistan
    TN(Continent.AF), // Tunisia
    TO(Continent.OC), // Tonga
    TR(Continent.AS), // Turkey
    TT(Continent.NA), // Trinidad and Tobago
    TV(Continent.OC), // Tuvalu
    TW(Continent.AS), // Taiwan
    TZ(Continent.AF), // Tanzania
    UA(Continent.EU), // Ukraine
    UG(Continent.AF), // Uganda
    US(Continent.NA), // United States
    UY(Continent.SA), // Uruguay
    UZ(Continent.AS), // Uzbekistan
    VA(Continent.EU), // Vatican City
    VC(Continent.NA), // Saint Vincent and the Grenadines
    VE(Continent.SA), // Venezuela
    VG(Continent.NA), // British Virgin Islands
    VI(Continent.NA), // U.S. Virgin Islands
    VN(Continent.AS), // Vietnam
    VU(Continent.OC), // Vanuatu
    WF(Continent.OC), // Wallis and Futuna
    WS(Continent.OC), // Samoa
    YE(Continent.AS), // Yemen
    YT(Continent.AF), // Mayotte
    ZA(Continent.AF), // South Africa
    ZM(Continent.AF), // Zambia
    ZW(Continent.AF), // Zimbabwe
    XK(Continent.EU), // Kossovo
    GS(Continent.SA),
    UM(Continent.OC),
    BL(Continent.NA),
    CW(Continent.NA),
    CX(Continent.OC);
    private final Continent continent;
    Country(Continent continent) {
        this.continent = continent;
    }

    public Continent getContinent() {
        return continent;
    }

    @Override
    public String toString() {
        return name() + " (" + continent + ")";
    }
}
